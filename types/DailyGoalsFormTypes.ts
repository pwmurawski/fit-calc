/* eslint-disable no-unused-vars */
export type Keys = "kcal" | "protein" | "fat" | "carbs";

export type KeysPercentMacro = "protein" | "fat" | "carbs";

export type OnPercentType = (data: number) => void;
